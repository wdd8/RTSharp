<UserControl x:Class="RTSharp.Views.ActionQueueView"
         xmlns="https://github.com/avaloniaui"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:vm="clr-namespace:RTSharp.ViewModels;assembly=RTSharp"
         d:DesignWidth="800" d:DesignHeight="400">

    <!--<ListBox Items="{Binding ActionQueue}" VirtualizationMode="None">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <ContentControl Content="{Binding Queue.Display}" />
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>-->
    <ItemsRepeater ItemsSource="{Binding QueueDisplay}">
    </ItemsRepeater>
</UserControl>
